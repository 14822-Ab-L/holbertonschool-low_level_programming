#include <stdio.h>

/**
 * main - Prints a string using the function puts
 *
 * Return: Always end with the value 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
